# Add these values in Vercel -> Project -> Settings -> Environment Variables
# Do not commit real secrets.

# Example format:
# https://yourportal.bitrix24.kz/rest/1/xxxxxxxxxxxx/
BITRIX_WEBHOOK_BASE_URL=

# Default CRM method. Can be changed later if we decide to create deals instead of leads.
BITRIX_METHOD=crm.lead.add.json
